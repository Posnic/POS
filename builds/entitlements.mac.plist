<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for the notarized build (RELEASE_TRUST_PLAN §3).

  Each one is here for a reason the app actually has:
    - allow-jit / allow-unsigned-executable-memory: Electron's V8.
    - disable-library-validation: native modules (serial scale readers,
      keytar's keychain bridge) are signed by their own builders, not by us.
    - allow-dyld-environment-variables: the bundled mongod child process.
    - network client+server: the till serves its own API on loopback and
      talks to the cloud when asked.
    - files.user-selected: import/export dialogs (CSV, backups).
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.network.client</key>
  <true/>
  <key>com.apple.security.network.server</key>
  <true/>
  <key>com.apple.security.files.user-selected.read-write</key>
  <true/>
</dict>
</plist>
