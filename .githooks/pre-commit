#!/bin/sh
# Security audit on what is staged. See security-audit.js for what it catches
# and why. Installed per clone with: git config core.hooksPath .githooks
set -e
HOOK_DIR=$(dirname "$0")
node "$HOOK_DIR/security-audit.js"
exec node "$HOOK_DIR/../scripts/check-ai-attribution.js" --staged
